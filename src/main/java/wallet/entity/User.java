package wallet.entity;


import java.util.UUID;

public class User {

    private UUID id;
    private String phoneNumber;
    //We can have additional fields like phone number, name, address, ID proof number etc.

}
